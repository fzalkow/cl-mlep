These files are only for users with no Quicklisp. `load.lisp` loads `mlep` and `load-with-add.lisp` loads `mlep` as well as `mlep-add`.
Maybe you should consider installing [Quicklisp](http://www.quicklisp.org)? 